{
  "name": "@vtg/data-layer",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "bundle": "dist/ts.yoki.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist/*",
    "build": "esbuild ${npm_package_main} --bundle --minify --format=iife --outfile=${npm_package_bundle}"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.21",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  }
}
